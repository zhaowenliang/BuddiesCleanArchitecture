<resources>

    <!-- Base application theme. AppCompat or MaterialComponents  -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:colorBackground">#FAFAFA</item>
    </style>

    <!-- 启动页主题 -->
    <style name="AppTheme.Splash">
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无ActionBar -->
        <item name="android:windowActionBar">false</item>
        <!-- 全屏即无通知栏 -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- 彩色标题栏主题 -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!-- 彩色标题栏弹窗菜单主题 -->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!-- ShapeableImageView 圆形样式 -->
    <style name="Widget.App.ShapeableImageView.Circle" parent="Widget.MaterialComponents.ShapeableImageView">
        <item name="shapeAppearance">?attr/shapeAppearanceSmallComponent</item>
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay.App.CornerSize50Percent
        </item>
    </style>

</resources>